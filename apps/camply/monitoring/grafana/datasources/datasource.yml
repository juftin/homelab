# Grafana Datasource Provisioning — Prometheus
apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://camply-prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: "15s"
