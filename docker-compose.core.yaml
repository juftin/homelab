# yaml-language-server: $schema=https://raw.githubusercontent.com/compose-spec/compose-go/refs/heads/main/schema/compose-spec.json

################################################################################
# HOMELAB-CORE
################################################################################

name: homelab-core

include:
    - core/traefik/docker-compose.yaml # Traefik (Reverse Proxy)
    - core/cloudflare-ddns.yaml # Cloudflare-DDNS (Dynamic DNS Updater)
    - core/oauth.yaml # traefik-forward-auth (Google OAuth2)
    - core/socket-proxy.yaml # Docker Socket Proxy (Security)
    - core/komodo/docker-compose.yaml # Komodo (Docker Management Platform)

################################################################################
# NETWORK CONFIGURATION
################################################################################

networks:
    traefik:
        driver: bridge
        ipam:
            config:
                - subnet: 192.168.90.0/24
    docker:
        driver: bridge
        ipam:
            config:
                - subnet: 192.168.91.0/24
    internal:
        driver: bridge
        ipam:
            config:
                - subnet: 192.168.92.0/24
